name: Terraform Plan

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  terraform-plan:
    runs-on: self-hosted

    defaults:
      run:
        working-directory: env/test-bed

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Azure Login Check
        shell: powershell
        run: |
          az account show --output table

      - name: Terraform Version
        shell: powershell
        run: |
          terraform --version

      - name: Terraform Init
        shell: powershell
        run: |
          terraform init

      - name: Terraform Validate
        shell: powershell
        run: |
          terraform validate

      - name: Terraform Plan
        shell: powershell
        run: |
          terraform plan